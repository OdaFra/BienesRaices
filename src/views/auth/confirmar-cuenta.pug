extends ../layout/index

block contenido
    div.py-10
        h1.text-4xl.my-10.text-center
            span.font-normal Bienes 
            span.font-extrabold Raices
        h2.text-center.text-2xl.front-extrabold=pagina

    div(class="bg-white py-8 px-4 shadow rounded-lg max-w-md mx-auto")
        p(class = error ? 'bg-red-500': 'bg-green-500').py-2.px-5.rounded-lg.w-full.font-bold.text-center.text-white.uppercase= mensaje

        if !error
            a(href='/auth/login', class='bg-blue-500 text-white py-2 px-4 rounded-lg block text-center mt-4') Iniciar Sesión